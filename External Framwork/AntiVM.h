#pragma once
#include "Headers.h"

namespace AntiVM
{
	extern BOOLEAN CheckLoadedDLLs();
	extern BOOLEAN CheckRegKeys();
	extern BOOLEAN CheckDevices();
	extern BOOLEAN CheckWindows();
	extern BOOLEAN CheckProcess();
}